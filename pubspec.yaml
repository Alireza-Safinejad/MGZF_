name: MGZF
description: Factory Monitor App
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_info_plus: ^9.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/Gz.PNG
